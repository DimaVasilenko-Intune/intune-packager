# Server port (default: 3001)
PORT=3001

# Set to "production" for strict CORS, rate limiting (30/min), and combined logging
NODE_ENV=development

# Comma-separated list of allowed CORS origins (only enforced in production)
# CORS_ORIGINS=https://your-domain.com,chrome-extension://your-extension-id
